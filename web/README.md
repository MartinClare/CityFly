# City Sight web

Public magazine for **CS Magazine / City Sight**.

```bash
# from repo root
cd robot && source venv/bin/activate && python scripts/publish_site.py
cd ../web && npm install && npm run dev
```

Vercel: set project root to `web/`. Custom domain `www.citysight.net` after DNS cutover.
